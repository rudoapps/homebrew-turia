import com.google.gson.annotations.SerializedName

data class {{TEMPLATE_NAME}}Dto(
    @SerializedName("id") val id: Int,
    @SerializedName("name") val name: String
) {
    fun toDomain(): {{TEMPLATE_NAME}} {
        return {{TEMPLATE_NAME}}(
            id = id,
            name = name
        )
    }
}